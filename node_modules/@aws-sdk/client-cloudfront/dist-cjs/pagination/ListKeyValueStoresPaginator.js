"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paginateListKeyValueStores = void 0;
const CloudFrontClient_1 = require("../CloudFrontClient");
const ListKeyValueStoresCommand_1 = require("../commands/ListKeyValueStoresCommand");
const makePagedClientRequest = async (client, input, ...args) => {
    return await client.send(new ListKeyValueStoresCommand_1.ListKeyValueStoresCommand(input), ...args);
};
async function* paginateListKeyValueStores(config, input, ...additionalArguments) {
    let token = config.startingToken || undefined;
    let hasNext = true;
    let page;
    while (hasNext) {
        input.Marker = token;
        input["MaxItems"] = config.pageSize;
        if (config.client instanceof CloudFrontClient_1.CloudFrontClient) {
            page = await makePagedClientRequest(config.client, input, ...additionalArguments);
        }
        else {
            throw new Error("Invalid client, expected CloudFront | CloudFrontClient");
        }
        yield page;
        const prevToken = token;
        token = page.KeyValueStoreList.NextMarker;
        hasNext = !!(token && (!config.stopOnSameToken || token !== prevToken));
    }
    return undefined;
}
exports.paginateListKeyValueStores = paginateListKeyValueStores;
